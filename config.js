export const config = {
    workingDirectory: `${__dirname}/../output`,
    dbName: "arh_dashboard_live",
    wasabi: {
        accessKey: "",
        secretKey: "",
        bucketName: ""
    },
    backupLog: `${__dirname}/../log/`
}